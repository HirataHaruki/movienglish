<div class="users-new-wrapper">
  <div class="container">
    <h1 class="text-center text-white">新規登録</h1>
    <%= form_for @user do |f|%>
      <div class="form-group">
        <%= f.label :名前, class: 'text-white' %>
        <%= f.text_field :name, class: 'form-control' %>
      </div>
      <div class="form-group">
        <%= f.label :Eメール, class: 'text-white' %>
        <%= f.text_field :email, class: 'form-control' %>
      </div>
      <div class="form-group">
        <%= f.label :パスワード（最小8文字）, class: 'text-white' %>
        <%= f.password_field :password, class: 'form-control' %>
      </div>
      <div class="form-group">
        <%= f.label :パスワード（確認のためもう一度入力）, class: 'text-white' %>
        <%= f.password_field :password_confirmation, class: 'form-control' %>
      </div>
      
      <%= f.submit "登録", class: 'btn btn-block btn-white' %>
    <% end %>
  </div>
</div>
